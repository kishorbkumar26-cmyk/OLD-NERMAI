{
  "name": "@nermai/shared",
  "version": "1.0.0",
  "description": "Shared React hooks and utilities for Web and Mobile",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@nermai/api": "*",
    "@nermai/types": "*"
  },
  "peerDependencies": {
    "react": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
