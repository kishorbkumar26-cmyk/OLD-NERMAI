{
  "name": "@nermai/interaction",
  "version": "1.0.0",
  "description": "Shared Interaction Engine business logic for NERMAI Academy",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@nermai/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
